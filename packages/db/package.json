{
  "name": "@ganaka/db",
  "version": "1.0.0",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "prisma:generate": "env-cmd -f ../../.env --silent prisma generate",
    "prisma:migrate": "env-cmd -f ../../.env --silent prisma migrate dev",
    "prisma:migrate:deploy": "env-cmd -f ../../.env --silent prisma migrate deploy",
    "prisma:studio": "env-cmd -f ../../.env --silent prisma studio",
    "prebuild": "env-cmd -f ../../.env --silent prisma generate",
    "build": "env-cmd -f ../../.env --silent tsc",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@prisma/client": "^6.19.1"
  },
  "devDependencies": {
    "@types/node": "^20.10.5",
    "dotenv": "^17.2.3",
    "prisma": "^6.19.1",
    "typescript": "^5.3.3"
  }
}
